const downloadStyles = {backgroundColor: "white", color: "black", width: "220px"}

function Buttons() {

  return (
    <section id="buttons">
      <button style={downloadStyles}>Download for Mac</button>
      <button>Open Discord in your browser</button>
    </section> 
  )
}

export default Buttons